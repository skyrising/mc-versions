{
  "type": "module",
  "scripts": {
    "update": "tsc -b && node bin/update.js",
    "build": "tsc -b &&rm -rf dist && node bin/build.js"
  },
  "dependencies": {
    "node-fetch": "^3.0.0",
    "yauzl": "^2.10.0"
  },
  "devDependencies": {
    "@types/yauzl": "^2.9.2",
    "typescript": "^4.4.3"
  }
}
