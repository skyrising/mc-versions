{
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "yarn build-ts && yarn build-analyzer",
    "build-ts": "tsc -b",
    "build-analyzer": "cd jar-analyzer && ./gradlew build && cd ..",
    "update": "yarn build && node bin/update.js",
    "publish": "yarn build-ts && rm -rf dist && node bin/publish.js"
  },
  "dependencies": {
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.9.6",
    "typescript": "^4.4.3"
  }
}
