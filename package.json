{
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "update": "yarn build && node bin/update.js",
    "publish": "yarn build && rm -rf dist && node bin/publish.js"
  },
  "dependencies": {
    "node-fetch": "^3.0.0",
    "yauzl": "^2.10.0"
  },
  "devDependencies": {
    "@types/yauzl": "^2.9.2",
    "typescript": "^4.4.3"
  }
}
