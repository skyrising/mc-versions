{
  "type": "module",
  "scripts": {
    "build": "yarn build-ts && yarn build-analyzer",
    "build-ts": "tsc -b",
    "build-analyzer": "cd jar-analyzer && ./gradlew build && cd ..",
    "update": "yarn build && node bin/update.js",
    "publish": "yarn build && rm -rf dist && node bin/publish.js"
  },
  "dependencies": {
    "@types/node": "^16.9.6",
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^4.4.3"
  }
}
